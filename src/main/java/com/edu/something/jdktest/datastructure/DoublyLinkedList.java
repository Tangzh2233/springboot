/*
package com.edu.something.jdktest.datastructure;

import java.util.ArrayList;
import java.util.LinkedList;

*/
/**
 * @author Tangzhihao
 * @date 2017/12/11
 *//*


public class DoublyLinkedList {

    private Node prev;//保存本节点的前一个结点
    private Node node;

    public DoublyLinkedList(){
        node.prev=null;
        prev.next = null;
    }

    public void tailInsert(Object o){
        node.data = o;
        node.prev = prev;
        prev.next = node;
    }
    public void HeadInsert(Object o){
        node.data = o;
        node.prev = null;
    }

    public static void main(String[] args) {
        LinkedList<String> linkedList = new LinkedList<>();
        ArrayList<String> arrayList = new ArrayList<>();
        System.out.println(15 >> 1);
    }
    public static<T extends Comparable> T getMiddle(T... a){
        return a[a.length/2];
    }


    private static class Node<E>{
        Node<E> prev;
        Node<E> next;
        E data;
        Node(Node<E> prev,Node<E> next,E data){
            this.prev = prev;
            this.next = next;
            this.data = data;
        }
    }

}
*/
